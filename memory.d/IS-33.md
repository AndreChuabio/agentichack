# IS-33: Lessons learned

- **Commit-guard scopes:** The repo's commit-guard hook enforces Conventional Commits and rejects filenames as scopes. Use semantic scopes like `docs(fleet-contract)` for the area changed, not `docs(CLAUDE.md)`. This applies to all commits in the repo.
